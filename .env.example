# GitHub Packages Credentials (Required for Maven build)
# Copy these to a local .env file and replace with your real credentials
GITHUB_PACKAGES_USERNAME=your_github_username
GITHUB_PACKAGES_TOKEN=your_github_token

# Spring Profile
SPRING_PROFILES_ACTIVE=local

# Vault Token
VAULT_TOKEN=your_token_here
