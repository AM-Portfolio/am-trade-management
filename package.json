{
  "name": "am-trade-management",
  "version": "1.0.0",
  "description": "AM Trade Management System",
  "private": true,
  "scripts": {
    "vault:sync": "node scripts/vault-sync.js",
    "start": "set SPRING_PROFILES_ACTIVE=local && mvnw.cmd spring-boot:run -pl am-trade-app -s settings.xml",
    "build": "mvnw.cmd clean install -DskipTests -s settings.xml",
    "test": "mvnw.cmd test -s settings.xml"
  },
  "author": "AM-Portfolio",
  "license": "UNLICENSED"
}